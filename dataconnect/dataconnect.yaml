specVersion: "v1"
serviceId: "weekora_db"
location: "us-central1"
schema:
  source: "./schema"
datasource:
  postgresql:
    database: "weekora_db"
    cloudSql:
      instanceId: "weekora-pg-instance"
connectorDirs:
  - "./connector"
